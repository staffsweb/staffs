{% set titleElement = titleElement ?: 'span' %}

<details class="accordion__item">
  <summary class="accordion__title">
    <{{ titleElement }} class="accordion__toggle">{{ title }}</{{ titleElement }}>
  </summary>

  <div class="accordion__content">
    {{ content }}
  </div>
</details>
